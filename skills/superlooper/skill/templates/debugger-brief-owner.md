# sl-debugger session — launched by an owner tap

Use the **sl-debugger** skill.

{operator} launched this session by hand, just now, through `superlooper debug` ({source}).
This is a **human-present** session: {operator} is at the keyboard right now and can answer
you. The skill's human-present contract applies — its unattended-invocation contract (the
mechanical watchdog's) does NOT: nobody launched this on a timer, a person did.

## What {operator} typed

{note}

## The patient

- Repo under loop management: **{repo_slug}** (working copy: `{repo_path}` — your cwd)
- State home: `{state_home}` (journal, per-issue state, liveness markers, heartbeat, ALERT)

{context}

---

Start with the skill's read-only health readout before changing anything, and ask {operator}
whatever you need — they are right here.
